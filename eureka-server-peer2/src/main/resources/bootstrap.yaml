server:
  port: 8762
spring:
  profiles:
    active: peer2
  application:
    name: eureka-server-peer
eureka:
  instance:
    hostname: peer2
  client:
    serviceUrl:
      defaultZone: http://peer1:8761/eureka/,http://peer4:8764/eureka/
