server:
  port: 8762
spring:
  profiles:
    active: peer2
  application:
    name: eureka-server-peer2
eureka:
  instance:
    hostname: peer2
  client:
    serviceUrl:
      defaultZone: http://peer1:8761/eureka/